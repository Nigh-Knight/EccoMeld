---
phase: 03-js-bridge
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - app/build.gradle.kts
  - app/src/test/kotlin/com/metrolist/music/bridge/BridgeViewModelTest.kt
  - app/src/test/kotlin/com/metrolist/music/bridge/MeldBridgeInterfaceTest.kt
autonomous: true
requirements: [BRDG-02, BRDG-03]
must_haves:
  truths:
    - "JUnit 4 testImplementation dependency is wired in app/build.gradle.kts"
    - "BridgeViewModelTest.kt exists with failing stub tests for startBridge behavior"
    - "MeldBridgeInterfaceTest.kt exists with failing stub tests for JS interface callbacks"
    - "./gradlew app:testFossDebugUnitTest compiles test sources successfully (stubs fail with TODO assertions)"
  artifacts:
    - path: "app/build.gradle.kts"
      provides: "testImplementation(libs.junit) dependency"
      contains: "testImplementation(libs.junit)"
    - path: "app/src/test/kotlin/com/metrolist/music/bridge/BridgeViewModelTest.kt"
      provides: "Test stubs for BRDG-02 bridge invocation behavior"
      contains: "class BridgeViewModelTest"
    - path: "app/src/test/kotlin/com/metrolist/music/bridge/MeldBridgeInterfaceTest.kt"
      provides: "Test stubs for BRDG-03 JS interface callback behavior"
      contains: "class MeldBridgeInterfaceTest"
  key_links: []
---

<objective>
Create Wave 0 test infrastructure: add JUnit 4 dependency and scaffold test stubs for BridgeViewModel and MeldBridgeInterface. These stubs define the expected behavior contracts that Plans 01 and 02 must satisfy.

Purpose: Satisfy Nyquist validation requirement — test files must exist before implementation plans execute.
Output: Two test files with stub tests (marked @Ignore or failing with TODO), JUnit dependency added.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-js-bridge/03-CONTEXT.md
@.planning/phases/03-js-bridge/03-VALIDATION.md

<interfaces>
<!-- Key types the test stubs will reference -->

From app/src/main/kotlin/com/metrolist/music/ui/screens/bridge/BridgeScreen.kt:
```kotlin
sealed class BridgeUiState {
    @Immutable object Idle : BridgeUiState()
    @Immutable data class Searching(val foundHops: Int = 0, val totalHops: Int = 0) : BridgeUiState()
    @Immutable data class PathFound(val path: List<String>) : BridgeUiState()
    @Immutable data class PlaylistReady(val path: List<String>, val nowPlayingIndex: Int = 0) : BridgeUiState()
    @Immutable data class Error(val message: String) : BridgeUiState()
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add JUnit testImplementation dependency to app/build.gradle.kts</name>
  <files>app/build.gradle.kts</files>
  <read_first>
    - app/build.gradle.kts (find the dependencies block to add testImplementation)
    - gradle/libs.versions.toml (confirm libs.junit alias exists — it does: junit = { module = "junit:junit", version.ref = "junit" })
  </read_first>
  <action>
    In `app/build.gradle.kts`, locate the `dependencies { }` block and add:
    ```kotlin
    testImplementation(libs.junit)
    ```
    Place it near other test-related dependencies if any exist, or at the end of the dependencies block before the closing brace.

    This wires JUnit 4.13.2 (from libs.versions.toml) for local unit tests.
  </action>
  <verify>
    <automated>cd /home/kepler/Projects/EccoMeld && grep "testImplementation(libs.junit)" app/build.gradle.kts</automated>
  </verify>
  <done>app/build.gradle.kts contains testImplementation(libs.junit) and Gradle sync resolves it</done>
</task>

<task type="auto">
  <name>Task 2: Create test stub files for BridgeViewModel and MeldBridgeInterface</name>
  <files>app/src/test/kotlin/com/metrolist/music/bridge/BridgeViewModelTest.kt, app/src/test/kotlin/com/metrolist/music/bridge/MeldBridgeInterfaceTest.kt</files>
  <action>
    Create directory `app/src/test/kotlin/com/metrolist/music/bridge/`.

    Create `BridgeViewModelTest.kt` with these stub tests (all marked `@Ignore` with reason "Wave 0 stub — implementation in Plan 02"):

    ```kotlin
    package com.metrolist.music.bridge

    import org.junit.Ignore
    import org.junit.Test

    class BridgeViewModelTest {

        @Ignore("Wave 0 stub — implementation in Plan 02")
        @Test
        fun startBridge_rejects_when_running() {
            // BRDG-02: When a bridge is already running (isRunning == true),
            // calling startBridge() should be a no-op — no evaluateJavascript call.
        }

        @Ignore("Wave 0 stub — implementation in Plan 02")
        @Test
        fun startBridge_calls_evaluateJavascript() {
            // BRDG-02: Calling startBridge("Radiohead", "Lil Kim") should:
            // 1. Set uiState to BridgeUiState.Searching()
            // 2. Call evaluateJavascript with window.startBridge("Radiohead", "Lil Kim")
            // 3. Escape artist names via JSONObject.quote()
        }
    }
    ```

    Create `MeldBridgeInterfaceTest.kt` with these stub tests (all marked `@Ignore`):

    ```kotlin
    package com.metrolist.music.bridge

    import org.junit.Ignore
    import org.junit.Test

    class MeldBridgeInterfaceTest {

        @Ignore("Wave 0 stub — implementation in Plan 01")
        @Test
        fun createPlaylist_valid_json_emits_PathFound() {
            // BRDG-03: Calling createPlaylist('{"found":true,"path":["A","B","C"]}')
            // should invoke onStateChange with BridgeUiState.PathFound(["A","B","C"])
        }

        @Ignore("Wave 0 stub — implementation in Plan 01")
        @Test
        fun createPlaylist_not_found_emits_Error() {
            // BRDG-03: Calling createPlaylist('{"found":false,"path":[]}')
            // should invoke onStateChange with BridgeUiState.Error(...)
        }

        @Ignore("Wave 0 stub — implementation in Plan 01")
        @Test
        fun onProgress_emits_Searching() {
            // BRDG-03: Calling onProgress('{"depth":2,"maxDepth":4}')
            // should invoke onStateChange with BridgeUiState.Searching(foundHops=2, totalHops=4)
        }
    }
    ```

    These stubs use `@Ignore` so they compile and appear in test reports as "skipped" rather than failing. Plans 01 and 02 executors will remove `@Ignore` and fill in the test bodies as they implement the production code.
  </action>
  <verify>
    <automated>cd /home/kepler/Projects/EccoMeld && test -f app/src/test/kotlin/com/metrolist/music/bridge/BridgeViewModelTest.kt && test -f app/src/test/kotlin/com/metrolist/music/bridge/MeldBridgeInterfaceTest.kt && echo "OK"</automated>
  </verify>
  <done>Both test stub files exist with @Ignore-annotated test methods covering all 5 VALIDATION.md test cases</done>
</task>

</tasks>

<verification>
- `grep "testImplementation(libs.junit)" app/build.gradle.kts` shows dependency
- `find app/src/test -name "*Test.kt"` shows both test files
- `./gradlew app:testFossDebugUnitTest --tests "com.metrolist.music.bridge.*" -x lint` compiles and reports 5 skipped tests
</verification>

<success_criteria>
- app/build.gradle.kts has testImplementation(libs.junit)
- BridgeViewModelTest.kt exists with 2 @Ignore stub tests for BRDG-02
- MeldBridgeInterfaceTest.kt exists with 3 @Ignore stub tests for BRDG-03
- Test sources compile successfully via Gradle
</success_criteria>

<output>
After completion, create `.planning/phases/03-js-bridge/03-00-SUMMARY.md`
</output>
